json_key_file(ENV["JSON_KEY_FILE"]) # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("au.edu.faims.fieldmark") # e.g. com.krausefx.app


for_platform :android do
	for_lane :deploy_fieldmark_closed_track do
		package_name("au.edu.faims.fieldmark") # e.g. com.krausefx.app

	end
end
