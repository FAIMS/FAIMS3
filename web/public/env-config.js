// Placeholder runtime environment configuration script for local dev / static fallback.
// In container environments, generate-env-config.sh dynamically replaces this file with container env vars.
window.__ENV__ = window.__ENV__ || {};
