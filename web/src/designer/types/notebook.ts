/** Re-exports notebook-related types from `state/initial` for feature modules. */
export type {
  AppState,
  Notebook,
  NotebookMetadata,
  NotebookUISpec,
  NotebookWithHistory,
  FieldType,
  ComponentParameters,
} from '../state/initial';
