{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "release": {
            "dependsOn": [
                "^release"
            ],
            "env": [
                "GITHUB_TOKEN"
            ]
        }
    }
}