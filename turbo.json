{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint": {},
    "fix": {},
    "clean": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build", "dist"]
    }
  }
}