{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "lint": {},
        "fix": {},
        "clean": {
            "cache": false
        },
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                "build",
                "dist"
            ]
        }
    }
}
