apiVersion: 1

providers:
  - name: faims-load-test
    folder: FAIMS Load Tests
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
