json_key_file(ENV["ANDROID_JSON_KEY_FILE"]) # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name(ENV["ANDROID_APP_ID"])


for_platform :android do
	for_lane :deploy_fieldmark do
		package_name(ENV["ANDROID_APP_ID"])
	end
end
